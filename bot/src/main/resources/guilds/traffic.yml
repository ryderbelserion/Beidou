# Traffic related settings.
traffic:
  # The name of the module.
  join-module:
    # Role settings.
    role-settings:
      # Should we give a role(s) on join?
      enabled: false
      # A list of role ids to give.
      roles: []
    # List of channels to send the message to.
    join-channel: []
    # The name of the embed i.e. from the `embeds` folder!
    embed: ""